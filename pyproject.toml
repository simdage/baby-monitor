[project]
name = "baby-monitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pip>=25.3",
    "pyaudio>=0.2.14",
    "torch>=2.9.1",
    "torchaudio>=2.9.1",
]
