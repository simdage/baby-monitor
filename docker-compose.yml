version: '3.8'

services:
  baby-monitor:
    build: .
    container_name: baby-monitor
    # Mount ALSA devices for Linux audio access
    devices:
      - /dev/snd:/dev/snd
    # Add group access for audio devices
    group_add:
      - audio
    stdin_open: true
    tty: true

